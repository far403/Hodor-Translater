#include <stdio.h>
#include <stdlib.h>


void hodor(char word[]);

int main()
{
    char order[50];

printf("Give an Order to Hodor!\n");
printf("Order : ");

gets(order);
hodor(order);

    return 0;
}

void hodor(char word[])
{
    word="Hodor";
printf("Translating to Hodor: %s\n",word);

}
